<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sundata.login.admin.mapper.AdminLoginMapper">

	<select id="login" resultType="com.sundata.login.admin.model.AdminLoginModel">
		select * from 	tb_admin  where account =#{account} and password = #{password}

	</select>

</mapper>